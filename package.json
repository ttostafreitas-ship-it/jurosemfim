{
  "name": "meu-financeiro",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de organizacao financeira domestica (offline-first, IndexedDB, sem servidor).",
  "scripts": {
    "build": "terser assets/db.js -o assets/db.js -c -m --format comments=false && terser assets/app.js -o assets/app.js -c -m --format comments=false && terser assets/relatorio.js -o assets/relatorio.js -c -m --format comments=false && terser assets/historico.js -o assets/historico.js -c -m --format comments=false"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
